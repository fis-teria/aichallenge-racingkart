#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${ROOT_DIR}/tools/aic_test${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m aic_test --repo-root "${ROOT_DIR}" "$@"
